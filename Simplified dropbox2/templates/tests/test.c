#define CUT_MAIN

#include "cut.h"

/* EXAMPLE

TEST(add)
{
        SUBTEST(good)
        {
                ASSERT(1 + 2 == 3);
        }

        SUBTEST(bad)
        {
                ASSERT(1 + 1 == 3);
        }
}
*/